import React from 'react'
//import {PageNotFound, PageSoon} from '../components/' | SOON ! 

export default function MainPage() {
  return (
    <div>
        <header>
            {/* ? YAO ? */}
        </header>
    </div>
  )
}
